{
  "name": "moove",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/*",
    "packages/*",
    "legacy/api"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:all": "concurrently -n API,WEB -c green,blue \"cd legacy/api && PORT=3005 npx tsx watch src/index.ts\" \"cd apps/client && PORT=3000 next dev --hostname 0.0.0.0 --port 3000\"",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "concurrently": "^9.1.0"
  }
}